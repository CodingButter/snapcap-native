node_modules/
dist/
*.tsbuildinfo

# VitePress build artifacts
docs/.vitepress/cache/
docs/.vitepress/dist/

# Vendored Snap bundle — not committed; regenerated by scripts/download-bundle.sh
vendor/

# Local credentials (never commit)
.env
.env.local
*.key
*.pem
.snapcap-smoke.json

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
*~

# Logs
*.log
.nyc_output/
coverage/

# Local scratch (auth state, recon HARs, etc) — never commit
.tmp/

# Dev probe scripts that hardcode test creds — keep local, don't ship
scripts/test-bundle-auth.ts

# Claude Code per-user state. Keep .claude/ tracked for shared directives
# (e.g. .claude/doc_guide_description.md), but ignore user-specific state.
.claude/settings.local.json
.claude/sessions/
.claude/.cache/
.claude/worktrees/
.claude/.last-conversation

docs-generated/
