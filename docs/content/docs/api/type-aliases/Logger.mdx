---
title: Logger
---

# Type Alias: Logger

```ts
type Logger = (event: LogEvent) => void;
```

Defined in: [logging.ts:108](https://github.com/CodingButter/snapcap-native/blob/e5c6ef042baa7d36bf620eee6b725ba6bbed1720/src/logging.ts#L108)

Handler signature passed to [setLogger](../functions/setLogger.mdx).

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `event` | [`LogEvent`](LogEvent.mdx) | One emitted [LogEvent](LogEvent.mdx). Switch on `event.kind` to narrow to a specific variant. |

## Returns

`void`

## Remarks

Handlers should return synchronously and avoid throwing — exceptions
thrown from a handler are swallowed by the internal emit point so a bad
logger can never break the network path, but they will be silently
dropped.
