---
title: Friends
---

# Class: Friends

Defined in: [api/friends.ts:957](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L957)

Concrete [IFriendsManager](../interfaces/IFriendsManager.mdx) implementation.

Constructed once per [SnapcapClient](SnapcapClient.mdx) and held as
[SnapcapClient.friends](SnapcapClient.mdx#friends). See [IFriendsManager](../interfaces/IFriendsManager.mdx) for the
full method-level documentation.

## See

[IFriendsManager](../interfaces/IFriendsManager.mdx)

## Implements

- [`IFriendsManager`](../interfaces/IFriendsManager.mdx)

## Methods

### acceptRequest()

```ts
acceptRequest(userId: string): Promise<void>;
```

Defined in: [api/friends.ts:1003](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1003)

Accept an incoming friend request.

Equivalent on the wire to [IFriendsManager.sendRequest](../interfaces/IFriendsManager.mdx#sendrequest) with
`source: ADDED_BY_ADDED_ME_BACK` — the SPA path. Surfaced as a named
verb because the inbox flow has its own consumer mental model;
`acceptRequest(req.fromUserId)` reads more clearly than
`sendRequest(req.fromUserId, { source: 4 })`.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `userId` | `string` | Hyphenated UUID of the requester whose request to accept (the `fromUserId` field on a [ReceivedRequest](../interfaces/ReceivedRequest.mdx)). |

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<`void`\>

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`acceptRequest`](../interfaces/IFriendsManager.mdx#acceptrequest)

***

### block()

```ts
block(userId: string): Promise<void>;
```

Defined in: [api/friends.ts:991](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L991)

Block a user — also removes any existing friend link.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `userId` | `string` | Hyphenated UUID of the user to block. |

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<`void`\>

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`block`](../interfaces/IFriendsManager.mdx#block)

***

### getUsers()

```ts
getUsers(userIds: string[], opts?: {
  refresh?: boolean;
}): Promise<FriendsUser[]>;
```

Defined in: [api/friends.ts:1118](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1118)

Resolve a list of user IDs to [User](../interfaces/FriendsUser.mdx) records (username +
display name).

Cache-first: each ID is looked up in the bundle's
`state.user.publicUsers` cache; only IDs that miss are sent to Snap's
`GetSnapchatterPublicInfo`. Pass `{ refresh: true }` to force a
fresh RPC for every ID.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `userIds` | `string`[] | Hyphenated UUIDs to resolve. |
| `opts?` | \{ `refresh?`: `boolean`; \} | `refresh: true` re-fetches all IDs, ignoring the cache. Defaults to cache-first. |
| `opts.refresh?` | `boolean` | - |

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<[`FriendsUser`](../interfaces/FriendsUser.mdx)[]\>

Resolved [User](../interfaces/FriendsUser.mdx) records in the same order as `userIds`.
IDs the server returned no record for (deleted accounts, blocks)
appear with `notFound: true`.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`getUsers`](../interfaces/IFriendsManager.mdx#getusers)

***

### list()

```ts
list(): Promise<Friend[]>;
```

Defined in: [api/friends.ts:1103](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1103)

All friends in the logged-in user's social graph (excluding self).

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<[`Friend`](../interfaces/Friend.mdx)[]\>

Mutually-confirmed friends as [Friend](../interfaces/Friend.mdx) records.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`list`](../interfaces/IFriendsManager.mdx#list)

***

### on()

```ts
on<K>(
   event: K, 
   cb: FriendsEvents[K], 
   opts?: {
  signal?: AbortSignal;
}): Subscription;
```

Defined in: [api/friends.ts:1228](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1228)

Subscribe to a typed friends event. Returns a [Subscription](../type-aliases/Subscription.mdx)
— call it to unsubscribe, or use `sub.signal` to tie the
subscription's life to anything that takes an `AbortSignal`.

#### Type Parameters

| Type Parameter | Description |
| ------ | ------ |
| `K` *extends* keyof [`FriendsEvents`](../interfaces/FriendsEvents.mdx) |  |

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `event` | `K` | Event name from [FriendsEvents](../interfaces/FriendsEvents.mdx). |
| `cb` | [`FriendsEvents`](../interfaces/FriendsEvents.mdx)\[`K`\] | Callback fired with the event payload (type narrows on `event`). |
| `opts?` | \{ `signal?`: [`AbortSignal`](https://developer.mozilla.org/docs/Web/API/AbortSignal); \} | Optional `signal` — when the passed `AbortSignal` aborts, the subscription is torn down automatically. The returned `sub.signal` reflects the combined lifetime (fires on either path). |
| `opts.signal?` | [`AbortSignal`](https://developer.mozilla.org/docs/Web/API/AbortSignal) | - |

#### Returns

[`Subscription`](../type-aliases/Subscription.mdx)

A [Subscription](../type-aliases/Subscription.mdx) — a callable unsubscribe thunk with
`.signal` attached.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`on`](../interfaces/IFriendsManager.mdx#on)

***

### onChange()

```ts
onChange(cb: (snap: FriendsSnapshot) => void): Unsubscribe;
```

Defined in: [api/friends.ts:1214](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1214)

Fire `cb` whenever any part of the friend graph changes — mutuals,
incoming requests, or outgoing requests.

The callback receives a full [FriendsSnapshot](../interfaces/FriendsSnapshot.mdx) reflecting the
new state. Initial state is NOT replayed — call
[IFriendsManager.snapshot](../interfaces/IFriendsManager.mdx#snapshot) once after subscribing if you need
a baseline.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `cb` | (`snap`: [`FriendsSnapshot`](../interfaces/FriendsSnapshot.mdx)) => `void` | Subscriber invoked with the latest snapshot on every relevant change. |

#### Returns

[`Unsubscribe`](../type-aliases/Unsubscribe.mdx)

An `Unsubscribe` thunk; idempotent on repeat calls.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`onChange`](../interfaces/IFriendsManager.mdx#onchange)

***

### receivedRequests()

```ts
receivedRequests(): Promise<ReceivedRequest[]>;
```

Defined in: [api/friends.ts:1108](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1108)

All pending received friend requests.

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<[`ReceivedRequest`](../interfaces/ReceivedRequest.mdx)[]\>

Inbound [ReceivedRequest](../interfaces/ReceivedRequest.mdx) records waiting for accept /
reject / ignore.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`receivedRequests`](../interfaces/IFriendsManager.mdx#receivedrequests)

***

### refresh()

```ts
refresh(): Promise<void>;
```

Defined in: [api/friends.ts:1086](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1086)

Force an explicit re-sync from the server — pulls the latest mutuals
+ outgoing requests (via `SyncFriendData`) AND incoming requests (via
`IncomingFriendSync`).

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<`void`\>

#### Remarks

The bundle does NOT auto-poll for fresh state — the SPA's React layer
normally drives that cadence, and we don't load React. So consumers
who want event subscriptions like [IFriendsManager.on](../interfaces/IFriendsManager.mdx#on)(`"request:received"`)
to actually fire must drive their own refresh cadence. Common patterns:

- Call `refresh()` in a `setInterval` (every 10–30s for inbox-style
  monitoring, every 60s+ for less time-sensitive use cases).
- Call `refresh()` on demand right before a snapshot read.

Best-effort — failures are swallowed (the existing `userSlice.syncFriends`
pattern). Subsequent reads return whatever is in cache.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`refresh`](../interfaces/IFriendsManager.mdx#refresh)

***

### rejectRequest()

```ts
rejectRequest(userId: string): Promise<void>;
```

Defined in: [api/friends.ts:1009](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1009)

Reject (ignore) an incoming friend request.

Maps to Snap's `IgnoreFriends` RPC — the same path the SPA's
"Ignore" button uses. Once rejected, the request disappears from
[IFriendsManager.receivedRequests](../interfaces/IFriendsManager.mdx#receivedrequests).

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `userId` | `string` | Hyphenated UUID of the requester whose request to reject (the `fromUserId` field on a [ReceivedRequest](../interfaces/ReceivedRequest.mdx)). |

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<`void`\>

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`rejectRequest`](../interfaces/IFriendsManager.mdx#rejectrequest)

***

### remove()

```ts
remove(userId: string): Promise<void>;
```

Defined in: [api/friends.ts:985](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L985)

Remove a friend from the social graph.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `userId` | `string` | Hyphenated UUID of the friend to remove. |

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<`void`\>

#### Remarks

**Why this doesn't work:** Snap's web SPA itself doesn't expose
"Remove Friend" anywhere in its UI — friend mutations like remove,
block, and unblock are restricted to the mobile clients (iOS / Android)
and the server enforces this at the policy layer. We verified empirically:

- The request reaches the server: `RemoveFriends → 200 grpc=0`.
- The body encodes correctly (we tested with empty `pageSessionId`,
  a random UUID, and the real `sc-a-nonce` session cookie value —
  all yield the same outcome).
- The bundle's chat module never calls `RemoveFriends` from any
  code path — the SPA's right-click menu on a friend chat shows
  only `Message Notifications`, `Delete Chats`, `Clear from Chat Feed`.
- After the call, `friends.list()` still returns the supposedly-removed
  account as mutual on both sides (we tested symmetric removes too).

**What does work:** [IFriendsManager.sendRequest](../interfaces/IFriendsManager.mdx#sendrequest) (web supports
AddFriends), [IFriendsManager.acceptRequest](../interfaces/IFriendsManager.mdx#acceptrequest),
[IFriendsManager.rejectRequest](../interfaces/IFriendsManager.mdx#rejectrequest).

**Workarounds:** none from web. To actually unfriend an account, the
user must do it from the official mobile app.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`remove`](../interfaces/IFriendsManager.mdx#remove)

***

### search()

```ts
search(query: string): Promise<FriendsUser[]>;
```

Defined in: [api/friends.ts:1181](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1181)

Search Snap's user index by username / display-name fragment.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `query` | `string` | Free-text query — Snap's "Add Friends" search box matches both usernames and display names, with prefix and substring weighting. |

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<[`FriendsUser`](../interfaces/FriendsUser.mdx)[]\>

A list of matching [User](../interfaces/FriendsUser.mdx) records. Empty when `query`
is empty or no users match.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`search`](../interfaces/IFriendsManager.mdx#search)

***

### sendRequest()

```ts
sendRequest(userId: string, opts?: {
  source?: FriendSource;
}): Promise<void>;
```

Defined in: [api/friends.ts:979](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L979)

Send a friend request / add a user to the friend list.

Resolves once the server acknowledges.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `userId` | `string` | Hyphenated UUID of the user to add. |
| `opts?` | \{ `source?`: [`FriendSource`](../type-aliases/FriendSource.mdx); \} | Advanced overrides; ignore for the common case. The one knob is `source` — anti-spam attribution context (mirrors what the SPA stamps on the request to identify which UI surface triggered the add). Defaults to [FriendSource](../variables/FriendSource.mdx)`.ADDED_BY_USERNAME`. Override only if you're explicitly mimicking a different UX flow (QR-code add, deep-link add, etc.). |
| `opts.source?` | [`FriendSource`](../type-aliases/FriendSource.mdx) | - |

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<`void`\>

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`sendRequest`](../interfaces/IFriendsManager.mdx#sendrequest)

***

### sentRequests()

```ts
sentRequests(): Promise<SentRequest[]>;
```

Defined in: [api/friends.ts:1113](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1113)

All pending sent friend requests (the logged-in user's adds
waiting for the recipient to accept).

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<[`SentRequest`](../interfaces/SentRequest.mdx)[]\>

Outbound [SentRequest](../interfaces/SentRequest.mdx) records.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`sentRequests`](../interfaces/IFriendsManager.mdx#sentrequests)

***

### snapshot()

```ts
snapshot(): Promise<FriendsSnapshot>;
```

Defined in: [api/friends.ts:1044](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L1044)

Canonical point-in-time view of the friend graph — mutuals + pending
requests in both directions.

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<[`FriendsSnapshot`](../interfaces/FriendsSnapshot.mdx)\>

A [FriendsSnapshot](../interfaces/FriendsSnapshot.mdx) with all three slots populated.

#### Remarks

The split read accessors ([IFriendsManager.list](../interfaces/IFriendsManager.mdx#list),
[IFriendsManager.receivedRequests](../interfaces/IFriendsManager.mdx#receivedrequests),
[IFriendsManager.sentRequests](../interfaces/IFriendsManager.mdx#sentrequests)) all project from this.

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`snapshot`](../interfaces/IFriendsManager.mdx#snapshot)

***

### unblock()

```ts
unblock(userId: string): Promise<void>;
```

Defined in: [api/friends.ts:997](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L997)

Unblock a previously-blocked user.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `userId` | `string` | Hyphenated UUID of the user to unblock. |

#### Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<`void`\>

#### Implementation of

[`IFriendsManager`](../interfaces/IFriendsManager.mdx).[`unblock`](../interfaces/IFriendsManager.mdx#unblock)
