---
title: idbPut
---

# Function: idbPut()

```ts
function idbPut(
   dbName: string, 
   storeName: string, 
   key: string, 
value: unknown): Promise<void>;
```

Defined in: [storage/idb-utils.ts:189](https://github.com/CodingButter/snapcap-native/blob/3189675f17f92b8303422ec60867fb61b844da17/src/storage/idb-utils.ts#L189)

Write `value` into the sandbox's IndexedDB at `(dbName, storeName, key)`.

## Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `dbName` | `string` | IndexedDB database name. |
| `storeName` | `string` | Object store name within the database. Created on first use if absent. |
| `key` | `string` | Record key within the object store. |
| `value` | `unknown` | Any structured-cloneable value. |

## Returns

[`Promise`](https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Promise)\<`void`\>

## Example

```ts
await idbPut("snapcap", "fidelius", "identity", { pub, priv });
```

## See

 - [idbGet](idbGet.mdx)
 - [idbDelete](idbDelete.mdx)
