---
title: BitmojiPublicInfo
---

# Interface: BitmojiPublicInfo

Defined in: [api/friends.ts:122](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L122)

Bitmoji avatar identifiers. Used to render a user's bitmoji via Snap's
`images.bitmoji.com` CDN — the avatar id pairs with a sticker / pose
id to construct the URL.

## Remarks

All four ids are optional and frequently absent — only users who have
created a bitmoji surface any of them, and the scene / background slots
are only set when the user has customized those layers.

The exact semantic difference between the ids is partially captured
from the bundle and partially inferred — treat the inline notes below
as best-effort, not authoritative.

## See

[User.bitmojiPublicInfo](Friend.mdx#bitmojipublicinfo)

## Indexable

```ts
[k: string]: unknown
```

Forward-compat for fields Snap adds to the bitmoji envelope.

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="bitmojiavatarid"></a> `bitmojiAvatarId?` | `string` | Primary avatar id — the head/body construction the bitmoji CDN keys off. | [api/friends.ts:124](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L124) |
| <a id="bitmojibackgroundid"></a> `bitmojiBackgroundId?` | `string` | Background id — the backdrop layer rendered behind the avatar. | [api/friends.ts:130](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L130) |
| <a id="bitmojisceneid"></a> `bitmojiSceneId?` | `string` | Scene id — full-body pose composition (observed only on richer profiles). | [api/friends.ts:128](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L128) |
| <a id="bitmojiselfieid"></a> `bitmojiSelfieId?` | `string` | Selfie pose / expression id — the avatar's face crop used for profile chips. | [api/friends.ts:126](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L126) |
