---
title: SentRequest
---

# Interface: SentRequest

Defined in: [api/friends.ts:247](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L247)

An outbound friend request — the logged-in user has added this account
and is waiting for them to accept.

## Remarks

`toUsername` / `toDisplayName` are best-effort: populated only when
the recipient is already in the `publicUsers` cache (mutuals lookups,
prior search, etc.). Callers can always match on `toUserId`.

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="todisplayname"></a> `toDisplayName?` | `string` | Recipient's display name — only present when the public-users cache holds them. | [api/friends.ts:253](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L253) |
| <a id="touserid"></a> `toUserId` | `string` | Hyphenated UUID of the recipient. | [api/friends.ts:249](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L249) |
| <a id="tousername"></a> `toUsername?` | `string` | Recipient's Snap username — only present when the public-users cache holds them. | [api/friends.ts:251](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends.ts#L251) |
