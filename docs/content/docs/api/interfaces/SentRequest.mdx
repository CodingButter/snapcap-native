---
title: SentRequest
---

# Interface: SentRequest

Defined in: [api/friends/types.ts:214](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends/types.ts#L214)

An outbound friend request — the logged-in user has added this account
and is waiting for them to accept.

## Remarks

`toUsername` / `toDisplayName` are best-effort: populated only when
the recipient is already in the `publicUsers` cache (mutuals lookups,
prior search, etc.). Callers can always match on `toUserId`.

## Properties

| Property | Type | Description | Defined in |
| ------ | ------ | ------ | ------ |
| <a id="todisplayname"></a> `toDisplayName?` | `string` | Recipient's display name — only present when the public-users cache holds them. | [api/friends/types.ts:220](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends/types.ts#L220) |
| <a id="touserid"></a> `toUserId` | `string` | Hyphenated UUID of the recipient. | [api/friends/types.ts:216](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends/types.ts#L216) |
| <a id="tousername"></a> `toUsername?` | `string` | Recipient's Snap username — only present when the public-users cache holds them. | [api/friends/types.ts:218](https://github.com/CodingButter/snapcap-native/blob/main/src/api/friends/types.ts#L218) |
